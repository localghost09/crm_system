{
  "name": "crm-system",
  "version": "1.0.0",
  "private": true,
  "description": "CRM Pro — Enterprise CRM System",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "dev": "npm run dev --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
